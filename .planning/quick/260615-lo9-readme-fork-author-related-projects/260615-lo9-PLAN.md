# Quick Plan: Remove README Fork Author and Related Projects Sections

Quick id: 260615-lo9
Date: 2026-06-15
Status: planned

## Problem

`README.md` still exposes original fork repository author information in two public sections near the bottom of the file. The requested outcome is a surgical README-only cleanup: remove `## Related Projects` and `## Author`, preserve `## License`, and keep legal attribution files such as `LICENSE` and `NOTICE.md` unchanged.

## Context Read

- `AGENTS.md`
- `.planning/STATE.md`
- `README.md` lines 300-354, covering `## Maintainer Validation`, `## Related Projects`, `## Author`, and `## License`

Planning note: `scripts/validate-skill-package.mjs` and `scripts/validate-skill-package.test.mjs` were not needed to determine edit boundaries. The existing README already names their validation commands under `## Maintainer Validation`, and this plan keeps both commands in the required verification set.

## Scope

- Edit only `README.md`.
- Preserve `## Maintainer Validation` exactly unless line wrapping becomes necessary around the deleted area.
- Remove the complete `## Related Projects` section:
  - Start boundary: the line `## Related Projects`.
  - Included content: the three project links for `Ian Handdrawn PPT`, `Awesome Claude Code Skills`, and `Obsidian + Claude AI Second Brain`.
  - End boundary: the horizontal rule immediately after those three bullets.
- Remove the complete `## Author` section:
  - Start boundary: the line `## Author`.
  - Included content: Ian bio, one-person-company sentence, GitHub, X/Twitter, Website, WeChat, Email, and the QR image HTML block referencing `assets/ian-wechat-qr.jpg`.
  - End boundary: the horizontal rule immediately before `## License`.
- Preserve the `## License` heading and `MIT License. See [LICENSE](LICENSE).` text.
- Preserve all legal attribution files and assets, including `LICENSE`, `NOTICE.md`, and `assets/ian-wechat-qr.jpg`; this quick task is README-only.
- Preserve unrelated changes and do not revert user or other-agent work.

## Tasks

### Task 1: Remove README Fork-Author Sections

File: `README.md`

Action: Delete exactly the two README sections bounded above: `## Related Projects` and `## Author`, including their adjacent trailing horizontal rules. Leave the prior `Pre-release checks live in [RELEASE_CHECKLIST.md](RELEASE_CHECKLIST.md).` paragraph followed by a single horizontal rule and then `## License`. Keep the license section unchanged. Preserve all non-README files.

Verify:

```bash
node -e "const fs=require('fs');const s=fs.readFileSync('README.md','utf8');const license=s.indexOf('## License');if(license<0) throw new Error('missing License section');for(const token of ['## Related Projects','## Author','Ian Handdrawn PPT','Awesome Claude Code Skills','Obsidian + Claude AI Second Brain','helloianneo','@ianneo_ai','ianneo.xyz','ian-wechat-qr.jpg','hello.neoc@gmail.com']){if(s.includes(token)) throw new Error('removed marker still present: '+token);}if(!s.slice(license).includes('MIT License. See [LICENSE](LICENSE).')) throw new Error('license text changed or missing');console.log('README fork-author markers removed and License preserved');"
```

Done: `README.md` no longer contains the Related Projects section, the Author section, or original fork-author markers, and it still contains the License section.

### Task 2: Run Package and README Validation

Files: `README.md`

Action: Run the existing validation suite and whitespace check. If validation fails because the README validator expects any removed fork-author marker, update only README wording if possible within this task boundary; if validation requires script changes, stop and report the validator coupling as a scope-changing risk before editing scripts.

Verify:

```bash
node scripts/validate-skill-package.mjs
node --test scripts/validate-skill-package.test.mjs
git diff --check
grep -nE '^(## Related Projects|## Author)$|Ian Handdrawn PPT|Awesome Claude Code Skills|Obsidian \+ Claude AI Second Brain|helloianneo|@ianneo_ai|ianneo\.xyz|ian-wechat-qr\.jpg|hello\.neoc@gmail\.com' README.md && exit 1 || true
```

Done: Package validator passes, Node tests pass, whitespace check passes, and README grep confirms the removed fork-author markers are absent.

### Task 3: Commit the README-Only Implementation

Files: `README.md`, `.planning/quick/260615-lo9-readme-fork-author-related-projects/260615-lo9-SUMMARY.md`, `.planning/STATE.md`

Action: After Task 1 and Task 2 pass, create the quick task summary and update `.planning/STATE.md` through the normal GSD quick executor flow. Stage only the README implementation plus GSD quick bookkeeping generated by this task. Keep unrelated working tree changes unstaged. Use this implementation commit message:

`docs(readme): remove fork author sections`

Verify:

```bash
git status --short
git log -1 --pretty=%s
```

Done: The implementation commit uses the expected English Angular-style message, includes the README cleanup and quick-task bookkeeping only, and leaves unrelated changes preserved.

## Acceptance Criteria

- `README.md` removes `## Related Projects` and every bullet in that section.
- `README.md` removes `## Author`, the Ian bio/contact details, and the QR image block.
- `README.md` keeps `## License` and `MIT License. See [LICENSE](LICENSE).`.
- `LICENSE`, `NOTICE.md`, and existing attribution assets remain unchanged.
- Validation commands pass:

```bash
node scripts/validate-skill-package.mjs
node --test scripts/validate-skill-package.test.mjs
git diff --check
grep -nE '^(## Related Projects|## Author)$|Ian Handdrawn PPT|Awesome Claude Code Skills|Obsidian \+ Claude AI Second Brain|helloianneo|@ianneo_ai|ianneo\.xyz|ian-wechat-qr\.jpg|hello\.neoc@gmail\.com' README.md && exit 1 || true
```

- Implementation commit message is `docs(readme): remove fork author sections`.
