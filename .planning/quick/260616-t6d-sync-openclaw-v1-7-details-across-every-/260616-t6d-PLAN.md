# Quick Plan: Sync OpenClaw Across Every README

Quick id: 260616-t6d
Date: 2026-06-16
Status: planned

## Problem

The English `README.md` includes the v1.7 OpenClaw route, but localized README variants still describe only Xiaohei, Littlebox, Tom, Ferris, and Seal. All README variants must expose the same route inventory, output path, source/license authority, workflow, directory structure, and validation summary.

## Scope

- Update every localized README sibling: `README.ar.md`, `README.de.md`, `README.es.md`, `README.fr.md`, `README.ja.md`, `README.ko.md`, `README.pt.md`, `README.ru.md`, `README.tr.md`, `README.uk.md`, `README.zh-Hant.md`, and `README.zh.md`.
- Keep `README.md` as the English source of truth.
- Extend validation so `DOC-OPENCLAW-001` requires OpenClaw route markers in every `README*.md` file.
- Keep runtime skill behavior unchanged.

## Tasks

### Task 1: Sync README Variants

Files: all `README*.md` files.

Action:

- Add OpenClaw to the opening summary, route inventory, outputs, route cards, canonical packs, operational facts, quick examples, workflow, directory structure, and maintainer validation in every localized README.
- Keep literal route tokens, paths, status values, output paths, escaped docs tokens, and source/license markers exact.
- Preserve existing language selectors, code fences, local links, and section order.

Verify:

```bash
node scripts/validate-skill-package.mjs
```

### Task 2: Harden README Variant Validation

Files: `scripts/validate-skill-package.mjs`.

Action:

- Add a helper that discovers all root `README*.md` files.
- Update `DOC-OPENCLAW-001` to require OpenClaw route markers in every discovered README variant.

Verify:

```bash
node scripts/validate-skill-package.mjs
node --test scripts/validate-skill-package.test.mjs
git diff --check
```

### Task 3: Archive Quick Task

Files: `.planning/quick/260616-t6d-sync-openclaw-v1-7-details-across-every-/260616-t6d-SUMMARY.md`, `.planning/STATE.md`.

Action:

- Record changed surfaces, commit hash, and validation output.
- Add a quick task completion row to `.planning/STATE.md`.

Verify:

```bash
git status --short
git log -2 --oneline
```

## Acceptance Criteria

- Every `README*.md` file contains `OpenClaw`, `source-reviewed`, `ian-xiaohei-illustrations/references/ips/openclaw/source.md`, `assets/<article-slug>-openclaw/`, and `assets/&lt;article-slug&gt;-openclaw/`.
- The package validator fails if any README variant loses those OpenClaw markers.
- `node scripts/validate-skill-package.mjs`, `node --test scripts/validate-skill-package.test.mjs`, and `git diff --check` pass.
