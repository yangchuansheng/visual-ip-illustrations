---
quick_id: 260617-h41
description: Regenerate OpenClaw and Go Gopher README example images with visual-ip-illustrations
status: in_progress
created: 2026-06-17
---

# Quick Task 260617-h41: Regenerate OpenClaw and Go Gopher README Examples

## Goal

Replace the incorrect OpenClaw and Go Gopher English README gallery images with images generated through the Visual IP Illustrations route contracts.

## Scope

- Replace `examples/images-en/openclaw/*.png`.
- Replace `examples/images-en/gopher/*.png`.
- Preserve existing filenames and README links.
- Keep `gopher.png` as local visual authority for the Go Gopher route.
- Use the OpenClaw route markers from `references/ips/openclaw/` and the locally inspected OpenClaw logo asset for identity calibration.

## Tasks

1. Generate route-correct samples
   - Generate one image per concept per route through the host image generation capability.
   - Keep 16:9 horizontal sparse article illustration style.
   - Keep OpenClaw and Go Gopher as the active cognitive subject.

2. Replace public README assets
   - Save accepted outputs to the existing `examples/images-en/openclaw/` and `examples/images-en/gopher/` filenames.
   - Preserve all README gallery paths.

3. Verify
   - Inspect representative OpenClaw and Go Gopher outputs visually.
   - Check image dimensions and file existence.
   - Run `node scripts/validate-skill-package.mjs`.
   - Run `node --test scripts/validate-skill-package.test.mjs`.
   - Run `git diff --check`.

## Acceptance Criteria

- OpenClaw samples show red round body, side claw-like arms, two antennae, black eyes, cyan pupils, and short legs.
- Go Gopher samples show the root `gopher.png` identity markers: blue rounded upright body, small rounded ears, large simple eyes, black nose, buck teeth, beige muzzle and paws, compact limbs, black outline, and white sticker-like border.
- All 16 replacement images are valid PNG files with 16:9 layout.
- Repository validators and Node tests pass.
