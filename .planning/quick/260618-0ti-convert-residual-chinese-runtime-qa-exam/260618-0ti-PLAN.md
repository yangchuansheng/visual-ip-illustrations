# Quick Task 260618-0ti Plan: Convert Residual Chinese Runtime QA Examples

## Goal

Convert residual Chinese runtime text in `skills/visual-ip-illustrations/SKILL.md` to English and add validator coverage that prevents Han title examples from returning to the skill entrypoint.

## Tasks

1. Update `SKILL.md` runtime copy so QA title examples, generation markers, preservation markers, output contract labels, and route alias guidance are English.
2. Update `scripts/validate-skill-package.mjs` so `SKILL.md` has a dedicated runtime English guard and no longer allowlists Chinese runtime markers.
3. Update `scripts/validate-skill-package.test.mjs` with the new check ID, expected totals, and a fixture that rejects Han title examples in `SKILL.md`.

## Verification

- `node scripts/validate-skill-package.mjs`
- `LANGUAGE_SCAN_ENFORCE=1 node scripts/validate-skill-package.mjs`
- `node --test scripts/validate-skill-package.test.mjs`
- `git diff --check`
- `rg -n "[\\p{Han}]" skills/visual-ip-illustrations/SKILL.md`
