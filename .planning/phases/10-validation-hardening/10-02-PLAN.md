---
id: 10-02
phase: 10-validation-hardening
plan: "02"
plan_id: 10-02
type: execute
wave: 2
depends_on:
  - 10-01
dependencies:
  - 10-01
files_modified:
  - scripts/validate-skill-package.test.mjs
autonomous: true
requirements:
  - TVAL-02
  - TVAL-03
  - TVAL-04
  - TVAL-05
  - TVAL-06
  - TVAL-07
acceptance_criteria:
  - "Node built-in tests assert the final 47-check Phase 10 matrix."
  - "Node test suite reports 15 tests, 15 passes, and 0 failures."
  - "Fixture tests prove Tom route, pack, docs, metadata, leakage, public asset, and approval parser diagnostics."
must_haves:
  truths:
    - "Maintainer can run `node --test scripts/validate-skill-package.test.mjs` and see 15 passing tests."
    - "Tests lock the Phase 10 check ID order and full-pass validator summary."
    - "Tests mutate temporary fixture repositories to prove actionable Tom failure diagnostics."
  artifacts:
    - path: scripts/validate-skill-package.test.mjs
      provides: "Phase 10 Node test coverage for validator ordering, parser helpers, Tom fixture failures, approval parsing, and full pass output"
      contains: "validator fixture enforces public Tom asset approval parsing"
  key_links:
    - from: scripts/validate-skill-package.test.mjs
      to: scripts/validate-skill-package.mjs
      via: "spawnSync validator execution"
      pattern: "validate-skill-package.mjs"
    - from: scripts/validate-skill-package.test.mjs
      to: scripts/validate-skill-package.mjs
      via: "requiredCheckIds exact order"
      pattern: "BOUNDARY-TOM-LEAK-001"
---

<objective>
Harden Node test coverage for the Phase 10 validator matrix.

Purpose: Lock the new 47-check validator behavior with targeted fixture mutations and stable output assertions.
Output: `scripts/validate-skill-package.test.mjs` with 15 passing Node built-in tests.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-validation-hardening/10-CONTEXT.md
@.planning/phases/10-validation-hardening/10-RESEARCH.md
@.planning/phases/10-validation-hardening/10-PATTERNS.md
@.planning/phases/10-validation-hardening/10-VALIDATION.md
@.planning/phases/10-validation-hardening/10-01-SUMMARY.md
@scripts/validate-skill-package.mjs
@scripts/validate-skill-package.test.mjs
@ian-xiaohei-illustrations/references/routing.md
@ian-xiaohei-illustrations/references/ips/tom/prompt-template.md
@ian-xiaohei-illustrations/references/ips/tom/qa-checklist.md
@examples/prompts.md
@RELEASE_CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Phase 10 order, parser, and full-pass assertions</name>
  <files>scripts/validate-skill-package.test.mjs</files>
  <action>Update only `scripts/validate-skill-package.test.mjs` per D-02, D-03, D-12, D-14, D-17, and TVAL-07. Extend `requiredCheckIds` to include the seven new IDs from Plan 10-01 in exact validator order. Rename the stale full-pass test intent to the Phase 10 matrix and assert `Summary: total=47 passed=47 failed=0 skipped=0`. Extend parser-helper assertions for three route rows, Tom semicolon reference splitting through `splitRouteCell()`, raw and escaped Tom output tokens, and any exported public-sample approval parser from Plan 10-01. Keep the existing `node:test`, `assert/strict`, `spawnSync`, `cpSync`, and temp fixture style.</action>
  <verify>
    <automated>node --test scripts/validate-skill-package.test.mjs | tee /tmp/phase10-02-tests-task1.out && rg -n 'tests 15' /tmp/phase10-02-tests-task1.out && rg -n 'pass 15' /tmp/phase10-02-tests-task1.out && rg -n 'fail 0' /tmp/phase10-02-tests-task1.out</automated>
    <automated>node scripts/validate-skill-package.mjs | tee /tmp/phase10-02-validator-task1.out && rg -n 'Summary: total=47 passed=47 failed=0 skipped=0' /tmp/phase10-02-validator-task1.out</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Tests know the final Phase 10 check order, assert the 47-check validator summary, and preserve parser/helper coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add targeted Tom fixture failure tests</name>
  <files>scripts/validate-skill-package.test.mjs</files>
  <action>Add seven focused tests per D-11, D-13, and TVAL-02 through TVAL-07. Use temporary copied repositories and mutate one surface per test. Required test names: `validator fixture rejects Tom route metadata drift`; `validator fixture requires Tom canonical pack files`; `validator fixture reports Tom prompt marker drift`; `validator fixture reports Tom identity and QA marker drift`; `validator fixture reports Tom docs and agent metadata drift`; `validator fixture reports Tom leakage in non-Tom packs`; `validator fixture enforces public Tom asset approval parsing`. Each test must assert nonzero status for the failing fixture, the relevant check ID, a repo-relative file path, and missing marker/item wording. The public asset test must cover pending-state failure for a Tom-named public image and complete approved-record behavior for the parser policy from Plan 10-01. Keep fixture cleanup with `rmSync(..., { recursive: true, force: true })`.</action>
  <verify>
    <automated>node --test --test-name-pattern='validator fixture rejects Tom route metadata drift' scripts/validate-skill-package.test.mjs</automated>
    <automated>node --test --test-name-pattern='validator fixture requires Tom canonical pack files' scripts/validate-skill-package.test.mjs</automated>
    <automated>node --test --test-name-pattern='validator fixture reports Tom prompt marker drift' scripts/validate-skill-package.test.mjs</automated>
    <automated>node --test --test-name-pattern='validator fixture reports Tom identity and QA marker drift' scripts/validate-skill-package.test.mjs</automated>
    <automated>node --test --test-name-pattern='validator fixture reports Tom docs and agent metadata drift' scripts/validate-skill-package.test.mjs</automated>
    <automated>node --test --test-name-pattern='validator fixture reports Tom leakage in non-Tom packs' scripts/validate-skill-package.test.mjs</automated>
    <automated>node --test --test-name-pattern='validator fixture enforces public Tom asset approval parsing' scripts/validate-skill-package.test.mjs</automated>
    <automated>node --test scripts/validate-skill-package.test.mjs | tee /tmp/phase10-02-tests-task2.out && rg -n 'tests 15' /tmp/phase10-02-tests-task2.out && rg -n 'pass 15' /tmp/phase10-02-tests-task2.out && rg -n 'fail 0' /tmp/phase10-02-tests-task2.out</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Node tests prove all Tom validator surfaces with targeted fixture mutations and report 15 passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Temporary fixture repo -> validator subprocess | Mutated files produce failure diagnostics without changing the real repository. |
| Validator implementation -> tests | Check IDs, exported helpers, and output summaries become stable test contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | Fixture mutation tests | mitigate | Copy repo to temp directories, mutate one surface per test, assert check ID/path, and clean fixtures. |
| T-10-05 | Repudiation | Validator output order | mitigate | Lock `requiredCheckIds`, exact PASS-line order, and exact summary count. |
| T-10-SC | Tampering | Package installs | accept | Package installs are zero; tests use Node built-ins already present in the repository. |
</threat_model>

<verification>
<automated>node scripts/validate-skill-package.mjs | tee /tmp/phase10-02-final-validator.out && rg -n 'Summary: total=47 passed=47 failed=0 skipped=0' /tmp/phase10-02-final-validator.out</automated>
<automated>node --test scripts/validate-skill-package.test.mjs | tee /tmp/phase10-02-final-tests.out && rg -n 'tests 15' /tmp/phase10-02-final-tests.out && rg -n 'pass 15' /tmp/phase10-02-final-tests.out && rg -n 'fail 0' /tmp/phase10-02-final-tests.out</automated>
<automated>git diff --check</automated>
</verification>

<success_criteria>
- `requiredCheckIds` exactly matches validator output order.
- Full-pass test expects 47 passes and zero failures.
- Seven targeted Tom fixture tests exist and pass.
- Test command reports 15 tests, 15 passes, and 0 failures.
</success_criteria>

<output>
Create `.planning/phases/10-validation-hardening/10-02-SUMMARY.md` when done.
</output>
