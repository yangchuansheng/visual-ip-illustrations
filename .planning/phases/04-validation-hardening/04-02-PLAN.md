---
phase: "04-validation-hardening"
plan: 2
type: "execute"
wave: 2
depends_on:
  - "04-01"
files_modified:
  - "scripts/validate-skill-package.mjs"
autonomous: true
requirements:
  - "VALD-02"
  - "VALD-03"
  - "VALD-05"
must_haves:
  truths:
    - "D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16: The validator checks package shape, metadata, routes, references, paths, prompts, docs, attribution, legacy parity, IP markers, and rendered-image boundaries."
    - "D-22, D-23, D-24: The validator treats `examples/prompts.md` as text-only smoke prompt coverage for default Xiaohei, explicit Xiaohei, explicit Littlebox, and mixed-IP variants."
    - "D-17, D-18, D-19, D-20, D-21: Every required drift condition reports a stable check ID and contributes to nonzero exit semantics."
  artifacts:
    - path: "scripts/validate-skill-package.mjs"
      provides: "Full deterministic contract check matrix for the installable skill package"
      contains: "ROUTE-LB-001"
  key_links:
    - from: "scripts/validate-skill-package.mjs"
      to: "ian-xiaohei-illustrations/references/routing.md"
      via: "route table metadata checks"
      pattern: "ROUTE-TABLE-001"
    - from: "scripts/validate-skill-package.mjs"
      to: "examples/prompts.md"
      via: "manual smoke prompt fixture checks"
      pattern: "SMOKE-MIXED-001"
    - from: "scripts/validate-skill-package.mjs"
      to: "NOTICE.md"
      via: "attribution marker checks"
      pattern: "NOTICE-LB-001"
---

<objective>
Implement the full validation contract matrix.

Purpose: The validator must catch drift in the installable package shape, route metadata, required references, prompt placeholders, output paths, docs links, attribution, smoke prompts, and IP boundaries before Phase 5 documentation and release work.

Output: `scripts/validate-skill-package.mjs` with required checks for VALD-02, VALD-03, and VALD-05.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-hardening/04-CONTEXT.md
@.planning/phases/04-validation-hardening/04-RESEARCH.md
@.planning/phases/03-littlebox-vertical-slice/03-05-SUMMARY.md
@.planning/phases/03-littlebox-vertical-slice/03-VERIFICATION.md
@.planning/phases/04-validation-hardening/04-01-SUMMARY.md
@ian-xiaohei-illustrations/SKILL.md
@ian-xiaohei-illustrations/agents/openai.yaml
@ian-xiaohei-illustrations/references/routing.md
@README.md
@examples/prompts.md
@NOTICE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement package, skill, agent, route, reference, output, and legacy checks</name>
  <files>scripts/validate-skill-package.mjs</files>
  <behavior>
    - Missing required files fail `PKG-SHAPE-001`.
    - Broken `SKILL.md` frontmatter/runtime markers fail `SKILL-FM-001`, `SKILL-ROUTE-001`, `SKILL-REFS-001`, `SKILL-OUTPUT-001`, or `SKILL-GEN-001`.
    - Broken `agents/openai.yaml` shape fails `AGENT-SHAPE-001`.
    - Broken `routing.md` table columns, route rows, defaults, references, paths, or mixed-IP wording fail the matching `ROUTE-*` check ID.
    - Root Xiaohei compatibility heading or body drift fails `LEGACY-XH-001` or `LEGACY-XH-002`.
  </behavior>
  <action>Extend the check registry using the exact check IDs from the Phase 4 research matrix for D-05 through D-11. Add `PKG-SHAPE-001`; `SKILL-FM-001`; `SKILL-ROUTE-001`; `SKILL-REFS-001`; `SKILL-OUTPUT-001`; `SKILL-GEN-001`; `AGENT-SHAPE-001`; `ROUTE-TABLE-001`; `ROUTE-XH-001`; `ROUTE-LB-001`; `ROUTE-DEFAULT-001`; `ROUTE-REFS-001`; `ROUTE-PATHS-001`; `ROUTE-MIXED-001`; `REFS-XH-001`; `REFS-LB-001`; `LEGACY-XH-001`; and `LEGACY-XH-002`. Validate required files listed in D-05, `SKILL.md` contract markers from D-06, `openai.yaml` shape from D-07, route metadata from D-08, required references from D-09, output suffix/path mapping from D-10, and root-to-canonical Xiaohei parity from D-11. For route reference paths, split semicolon-delimited cells, strip Markdown code ticks, resolve under `ian-xiaohei-illustrations/`, and fail paths that escape the package boundary. Keep `agents/openai.yaml` validation to shape and `allow_implicit_invocation: true`; Phase 5 owns selectable-IP copy updates per D-28.</action>
  <verify>
    <automated>node scripts/validate-skill-package.mjs &amp;&amp; rg -n "PKG-SHAPE-001|SKILL-FM-001|SKILL-ROUTE-001|AGENT-SHAPE-001|ROUTE-TABLE-001|ROUTE-XH-001|ROUTE-LB-001|ROUTE-DEFAULT-001|ROUTE-REFS-001|ROUTE-PATHS-001|ROUTE-MIXED-001|REFS-XH-001|REFS-LB-001|LEGACY-XH-001|LEGACY-XH-002" scripts/validate-skill-package.mjs</automated>
  </verify>
  <done>The validator covers package shape, skill metadata/runtime markers, agent metadata shape, routing metadata, required references, output suffixes, mixed-IP route wording, canonical pack existence, and legacy Xiaohei parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement prompt, IP marker, docs, attribution, smoke, and boundary checks</name>
  <files>scripts/validate-skill-package.mjs</files>
  <behavior>
    - Missing Xiaohei prompt placeholders fail `PROMPT-XH-001`.
    - Missing Littlebox planning or prompt variables fail `PROMPT-LB-001` or `PROMPT-LB-002`.
    - Missing objective IP markers fail `IP-XH-001` or `IP-LB-001`.
    - Broken README/examples local links fail `DOC-LINKS-001`.
    - Missing raw or escaped path tokens fail `DOC-PATHS-001`.
    - Missing public route docs, NOTICE markers, smoke prompt coverage, or rendered Littlebox boundary markers fail their matching check IDs.
  </behavior>
  <action>Add the remaining required checks for D-12, D-13, D-14, D-15, D-16, D-22, D-23, and D-24. Implement `PROMPT-XH-001` against canonical and legacy Xiaohei prompt templates for the required brace placeholders and edit placeholder. Implement `PROMPT-LB-001` and `PROMPT-LB-002` for Littlebox planning fields, English prompt language, background colors, closed box identity, front-left view, side-seam twig arms, amber jagged tape, and user's-language label markers. Implement `IP-XH-001` and `IP-LB-001` across canonical IP pack files for the objective IP markers named in D-13. Implement `DOC-LINKS-001`, `DOC-PATHS-001`, and `DOC-ROUTES-001` for README and examples local link existence, raw plus HTML-escaped output path tokens, canonical pack paths, and `references/routing.md`. Implement `NOTICE-IAN-001` and `NOTICE-LB-001` for Ian and Littlebox attribution markers from D-15. Implement `SMOKE-DEFAULT-001`, `SMOKE-XH-001`, `SMOKE-LB-001`, and `SMOKE-MIXED-001` against `examples/prompts.md` as text-only fixture coverage. Implement `BOUNDARY-IMG-001` using sorted filename/path checks under `examples/images/` and `ian-xiaohei-illustrations/assets/examples/` for `littlebox`, `小盒`, and `carton`, without pixel inspection. Implement `BOUNDARY-P5-001` to document that Phase 4 validates metadata shape only while Phase 5 owns broad docs, release checklist, and `agents/openai.yaml` wording per D-28, D-29, and D-30.</action>
  <verify>
    <automated>node scripts/validate-skill-package.mjs &amp;&amp; rg -n "PROMPT-XH-001|PROMPT-LB-001|PROMPT-LB-002|IP-XH-001|IP-LB-001|DOC-LINKS-001|DOC-PATHS-001|DOC-ROUTES-001|NOTICE-IAN-001|NOTICE-LB-001|SMOKE-DEFAULT-001|SMOKE-XH-001|SMOKE-LB-001|SMOKE-MIXED-001|BOUNDARY-IMG-001|BOUNDARY-P5-001" scripts/validate-skill-package.mjs</automated>
  </verify>
  <done>The validator covers prompt placeholders, objective IP markers, docs links and path tokens, route docs, attribution, manual smoke prompt coverage, rendered Littlebox import boundary, and Phase 5 boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden diagnostics and full-matrix positive run</name>
  <files>scripts/validate-skill-package.mjs</files>
  <behavior>
    - The validator reports all discovered failures in one run.
    - Each failure message includes the check ID, file path or relation, expected marker/relation, and observed missing condition.
    - The current repository passes with zero failures.
  </behavior>
  <action>Review every required check added in this plan and make failure messages actionable per D-18 and D-21. Preserve stable check ordering and stable summary output per D-19 and D-20. Verify all checks run from the repository root without package installation, framework setup, browser, network, image generation, CI, release archive creation, rendered image import, or machine-readable asset manifest creation per D-25 through D-30. Keep the implementation in `scripts/validate-skill-package.mjs`; this phase reads repository docs and skill files as validation inputs.</action>
  <verify>
    <automated>node scripts/validate-skill-package.mjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>The full check matrix passes on the current repository and produces deterministic English output with zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route table -> validator contract | Human-edited Markdown metadata drives required reference and path checks. |
| Public docs -> validator contract | README and examples are parsed for local links, output tokens, and smoke prompt coverage. |
| Attribution docs -> release confidence | NOTICE and Littlebox pack markers are treated as release-blocking attribution evidence. |
| Asset filenames -> import boundary | Existing image paths are scanned to preserve the rendered Littlebox boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | `required_references` parsing | mitigate | Split and normalize route table reference cells, resolve under the skill package, and fail escaping or missing paths. |
| T-04-05 | Repudiation | Contract checks | mitigate | Use fixed check IDs from the research matrix and include path-specific failure messages. |
| T-04-06 | Information Disclosure | Docs link validation | mitigate | Validate local files only; ignore external URL reachability and avoid network calls. |
| T-04-07 | Tampering | Attribution markers | mitigate | Treat missing Ian or Littlebox attribution markers as required failures. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No external packages are installed; Package Legitimacy Audit records `none`. |
</threat_model>

<verification>
<automated>node scripts/validate-skill-package.mjs &amp;&amp; rg -n "PKG-SHAPE-001|ROUTE-LB-001|PROMPT-XH-001|NOTICE-LB-001|SMOKE-MIXED-001|BOUNDARY-IMG-001" scripts/validate-skill-package.mjs &amp;&amp; git diff --check</automated>
</verification>

<success_criteria>
- The validator implements all required check IDs from the Phase 4 research matrix.
- VALD-02 coverage includes `SKILL.md` frontmatter, agent metadata shape, route aliases/defaults/status/output suffixes, required references, legacy Xiaohei paths, prompt placeholders, output paths, README/examples links, route docs, and NOTICE attribution.
- VALD-03 coverage includes nonzero failure semantics for required files, route rows, aliases, defaults, output suffixes, required references, prompt variables, docs links, attribution markers, legacy parity, smoke prompts, and rendered-image boundary failures.
- VALD-05 coverage includes text-only smoke prompt checks for omitted-IP default Xiaohei, explicit Xiaohei, explicit Littlebox, and mixed-IP variants.
- The current repository passes `node scripts/validate-skill-package.mjs`.
</success_criteria>

<output>
Create `.planning/phases/04-validation-hardening/04-02-SUMMARY.md` when done.
</output>
