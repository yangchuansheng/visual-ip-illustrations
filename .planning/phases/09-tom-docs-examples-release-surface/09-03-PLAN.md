---
phase: 09-tom-docs-examples-release-surface
plan: "03"
plan_id: 09-03
type: execute
wave: 1
depends_on: []
dependencies: []
files_modified:
  - ian-xiaohei-illustrations/agents/openai.yaml
autonomous: true
requirements:
  - TDOC-03
  - TDOC-05
acceptance_criteria:
  - "Agent metadata names Xiaohei, Littlebox, and Tom."
  - "Agent metadata preserves Xiaohei as implicit default."
  - "Agent metadata keeps `policy.allow_implicit_invocation: true`."
must_haves:
  truths:
    - "Agent display copy names all three selectable visual IP routes."
    - "Omitted-IP behavior remains Xiaohei in metadata."
    - "Tom metadata is explicit, gated, and protected."
  artifacts:
    - path: ian-xiaohei-illustrations/agents/openai.yaml
      provides: "OpenAI/Codex display metadata for the three-route skill package"
      contains: "allow_implicit_invocation: true"
  key_links:
    - from: ian-xiaohei-illustrations/agents/openai.yaml
      to: ian-xiaohei-illustrations/SKILL.md
      via: "Metadata summary of controller default behavior"
      pattern: "Xiaohei|Littlebox|Tom"
---

<objective>
Update OpenAI/Codex agent metadata so the displayed skill surface names Xiaohei, Littlebox, and Tom while preserving Xiaohei as the implicit default.

Purpose: Close TDOC-03 and reinforce TDOC-05 in concise metadata copy.
Output: `ian-xiaohei-illustrations/agents/openai.yaml` display strings updated for the three-route package.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tom-docs-examples-release-surface/09-CONTEXT.md
@.planning/phases/09-tom-docs-examples-release-surface/09-RESEARCH.md
@.planning/phases/09-tom-docs-examples-release-surface/09-PATTERNS.md
@ian-xiaohei-illustrations/agents/openai.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update agent display strings for three routes</name>
  <files>ian-xiaohei-illustrations/agents/openai.yaml</files>
  <action>Update only `interface.display_name`, `interface.short_description`, and `interface.default_prompt` in `ian-xiaohei-illustrations/agents/openai.yaml`. Name Xiaohei, Littlebox, and Tom in display copy per D-14. Preserve Xiaohei as the omitted-IP default in both `short_description` and `default_prompt` per D-15. Mark Tom as explicit, gated, and protected with the exact `gated-authorized` token per D-16 and D-01. Keep copy concise because behavior rules live in `SKILL.md`.</action>
  <verify>
    <automated>rg -n 'Xiaohei' ian-xiaohei-illustrations/agents/openai.yaml</automated>
    <automated>rg -n 'Littlebox' ian-xiaohei-illustrations/agents/openai.yaml</automated>
    <automated>rg -n 'Tom' ian-xiaohei-illustrations/agents/openai.yaml</automated>
    <automated>rg -n 'gated-authorized' ian-xiaohei-illustrations/agents/openai.yaml</automated>
    <automated>rg -n '默认.*Xiaohei|Xiaohei.*默认|default.*Xiaohei|Xiaohei.*default' ian-xiaohei-illustrations/agents/openai.yaml</automated>
    <automated>rg -n '显式.*Tom|Tom.*显式|explicit.*Tom|Tom.*explicit' ian-xiaohei-illustrations/agents/openai.yaml</automated>
  </verify>
  <done>Metadata display copy names the three routes and describes Tom as explicit/gated while keeping Xiaohei as the default.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve implicit invocation policy</name>
  <files>ian-xiaohei-illustrations/agents/openai.yaml</files>
  <action>Preserve `policy.allow_implicit_invocation: true` exactly per D-16. Ensure the metadata distinguishes skill-level implicit invocation from route-level omitted-IP behavior by keeping the default route wording tied to Xiaohei per D-15 and D-02.</action>
  <verify>
    <automated>rg -n 'allow_implicit_invocation: true' ian-xiaohei-illustrations/agents/openai.yaml</automated>
    <automated>rg -n '^interface:' ian-xiaohei-illustrations/agents/openai.yaml</automated>
    <automated>rg -n '^policy:' ian-xiaohei-illustrations/agents/openai.yaml</automated>
  </verify>
  <done>`policy.allow_implicit_invocation: true` remains present and metadata default wording remains Xiaohei-specific.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent metadata -> skill invocation | Display strings influence how users invoke default and explicit routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Spoofing | `openai.yaml` display copy | mitigate | Name the three routes and preserve Xiaohei default plus explicit Tom wording. |
| T-09-06 | Elevation of Privilege | Tom metadata route implication | mitigate | Mark Tom as explicit, gated, and protected while keeping `allow_implicit_invocation` at skill level. |
| T-09-SC | Tampering | Package installs | accept | No package install is planned; validation uses existing local Node scripts and `rg` checks. |
</threat_model>

<verification>
<automated>node scripts/validate-skill-package.mjs</automated>
<automated>node --test scripts/validate-skill-package.test.mjs</automated>
<automated>git diff --check</automated>
<automated>rg -n 'Xiaohei / Littlebox / Tom|Xiaohei.*Littlebox.*Tom' ian-xiaohei-illustrations/agents/openai.yaml</automated>
<automated>rg -n 'protected|protected-character|受保护|gated-authorized' ian-xiaohei-illustrations/agents/openai.yaml</automated>
</verification>

<success_criteria>
- `openai.yaml` closes TDOC-03.
- Xiaohei remains the implicit default in user-facing metadata.
- Tom is named as explicit and gated/protected, supporting TDOC-05.
</success_criteria>

<output>
Create `.planning/phases/09-tom-docs-examples-release-surface/09-03-SUMMARY.md` when done.
</output>
